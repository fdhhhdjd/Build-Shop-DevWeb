import { Options } from '../types';
import { TestCommandResult } from '../../cli/commands/types';
import { Ecosystem, ScanResult, TestResult } from './types';
export declare function testEcosystem(ecosystem: Ecosystem, paths: string[], options: Options): Promise<TestCommandResult>;
export declare function selectAndExecuteTestStrategy(ecosystem: Ecosystem, scanResultsByPath: {
    [dir: string]: ScanResult[];
}, options: Options): Promise<[TestResult[], string[]]>;
